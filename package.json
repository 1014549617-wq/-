{
  "name": "deprivation",
  "version": "1.0.0",
  "description": "Project Deprivation // 剥夺计划 - 100天现实禁闭仪式",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "NODE_ENV=production node server/server.js",
    "dev": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
